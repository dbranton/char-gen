'use strict';

var userModule = angular.module('user', ['ui.bootstrap.dialog', 'generator.compile', 'ui.bootstrap.accordion']);

function User() {
    /*$scope.opts = {
        backdrop: true,
        keyboard: true,
        backdropClick: true,
        templateUrl: 'login'
    };

    $scope.openDialog = function() {
        var dialog = $dialog.dialog($scope.opts);
        dialog.open();
    };*/
}
